(All commands should be run from the `example` directory.)
Install:
  npm install express consolidate handlebars caesar
  sudo npm install -g coffee-script

Run: (Server will start on localhost:3000)
  coffee app.coffee
